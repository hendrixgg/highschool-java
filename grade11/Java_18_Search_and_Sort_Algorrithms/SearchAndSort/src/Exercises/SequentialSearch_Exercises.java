package Exercises;

public class SequentialSearch_Exercises {

    public static void main(String[] args) {
        Ex1();
        Ex2();
        Ex3();
    }

    /**
     * Suppose that the first example of seqSearchwas used to search an array. What
     * would the method return if item appeared in the list more than once?
     */
    private static void Ex1() {
        System.out.println("1.Suppose that the first example of seqSearch was used to search an array.");
        System.out.println("  What would the method return if item appeared in the list more than once?");
        System.out.println();
        System.out.println("Answer: The method would return the location of of the last item in the list");
        System.out.println("because it loops through the whole list to the end and only returns the");
        System.out.println("location after the loop is completed.");
        
    }

    /**
     * What changes should be made to the sequential search in the second example so
     * that it searches an array of values starting at the end and moving to
     * the beginning?
     */
    private static void Ex2() {
    }

    private static void Ex3() {
    }
}